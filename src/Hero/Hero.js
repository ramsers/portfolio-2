import React from 'react';
import './Hero.scss';

const Hero = () => {
    return(
        <section className="hero">
            <section className="hero__ctn">
                
            </section>
        </section>
    )
}

export default Hero;